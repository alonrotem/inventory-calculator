meta {
  name: resend account verification
  type: http
  seq: 6
}

post {
  url: {{romtech_server}}/users/send-user-account-verification-code
  body: json
  auth: inherit
}

body:json {
  {
    "email_address": "alrotem@gmail.com"
  }
}

script:post-response {
  if(res.body.access_token) {
    bru.setVar("auth_token", res.body.access_token);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
